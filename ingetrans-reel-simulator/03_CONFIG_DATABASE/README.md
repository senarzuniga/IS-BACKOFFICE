# Config Database

Contiene archivos YAML de configuración por entidad. `config_master.yaml` referencia todos los archivos y sirve como punto de entrada para overrides por escenario.

Estructura:
- `config_*.yaml` : archivos por entidad
- `config_master.yaml` : incluye y organiza todos los archivos
